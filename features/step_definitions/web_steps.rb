When /^(?:a user )?visits the application at "([^"]*)"$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^a user visits the application at "([^"]*)" using service ticket "([^"]*)"$/ do |arg1, arg2|
  pending # express the regexp above with the code you wish you had
end

Then /^the user should be able to access the application$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the user should not be able to access the application$/ do
  pending # express the regexp above with the code you wish you had
end
